# Setup Project Locally

## API Server Setup

**1. Change the directory to api.**

```
cd ./api
```

**2. Run the package installation cmd.**

```
pnpm install
```

**3. Run the dev server.**

```
pnpm dev
```
